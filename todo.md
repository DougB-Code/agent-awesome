# TODO

### Features

- Implement Apple Calendar sync as an external app plugin backed by a real MCP, command, or HTTP tool boundary. Keep it out of first-class UI code; the current app plugin template is only a package contract.

- Connect website-login credential profiles to an agent/browser automation boundary so D2L downloads can authenticate through approved tool calls without exposing raw secrets.
