# This file configures Doug's memory server tools for Slack-driven harness turns.
local-exec:
  enabled: false
  allow-persistent-approvals: false
mcp:
  enabled: true
  servers:
    - name: memory
      transport: streamable-http
      endpoint: http://127.0.0.1:8090/mcp
