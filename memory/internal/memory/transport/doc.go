// Package transport exposes the memory service through process boundaries.
//
// Use this package for MCP and operational HTTP handlers. It owns
// protocol-specific schemas and routing while delegating reusable tool argument
// normalization and memory behavior to narrower packages.
package transport
