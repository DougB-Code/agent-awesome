// Package normalize centralizes small string normalization rules.
//
// Use this package for shared trim, fallback, key, and list normalization that
// must behave the same across domain, graph, repository, and store packages.
package normalize
