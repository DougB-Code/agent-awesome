// Package definition loads and validates declarative runbook definitions.
//
// Use this package when reading user-authored YAML runbook files before they
// are installed into the runbook runtime. Definitions may compose registered
// actions but must not define executable code.
package definition
