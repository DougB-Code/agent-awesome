// Package config parses reusable MCP manager configuration fragments.
//
// Use this package from process entrypoints and embedded hosts when they need
// the same JSON server list contract without sharing command-line code.
package config
