// Package adapters applies deterministic edge adapters between workflow nodes.
//
// Use this package to turn a source envelope into the target input envelope for
// a graph edge using direct pass-through, artifact selection, constants, or an
// AA Mapping Spec.
package adapters
