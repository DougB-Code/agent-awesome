// Package jsondata contains small deterministic helpers for JSON-shaped data.
//
// Use this package when workflow packages need the same dotted-path lookup or
// JSON-schema subset validation without owning their own local dialect.
package jsondata
